# Text-to-Speech voice settings.
pitch = integer(default=50)
rate = integer(default=175)
voice = string(default='en')
encoding = string(default='mp3')

# Text-to-Speech server settings.
tts_host = string(default='localhost')
tts_port = integer(default=8081)
